name: docs-quality-gates

on:
  pull_request:
  push:
    branches:
      - {{cookiecutter.default_branch}}

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: "3.3"

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Make scripts executable
        run: chmod +x .arch-docs/scripts/*.sh

      - name: Check wiki-llm manifest
        run: ./.arch-docs/scripts/wiki-llm-sync.sh --check

      - name: Audit local standard
        run: ./.arch-docs/scripts/audit-standard.sh --fail-on-gap

      - name: Validate docs
        run: ./.arch-docs/scripts/validate-docs.sh

      - name: Validate links
        run: ./.arch-docs/scripts/validate-links.sh

      - name: Validate schemas
        run: ./.arch-docs/scripts/validate-schemas.sh

      - name: Validate mermaid
        run: ./.arch-docs/scripts/validate-mermaid.sh

      - name: Validate OpenAPI
        run: ./.arch-docs/scripts/validate-openapi.sh

      - name: Validate AsyncAPI
        run: ./.arch-docs/scripts/validate-asyncapi.sh
