# Ограничения

- Архитектурный репозиторий не хранит код продукта.
- CI выполняет только детерминированные проверки.
- LLM и review agents не запускаются в CI.
- Merge документации в `{{cookiecutter.default_branch}}` запрещён до завершения реализации.
